import * as React from 'react';
import './Cell.css';

function Cell({ style }) {
  return (
    <div style={style} className="table-cell-container">
      test text
    </div>
  );
}

export default Cell;
